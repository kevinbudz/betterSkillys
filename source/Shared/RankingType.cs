namespace Shared
{
    public enum RankingType : int
    {
        Regular = 0,
        Donator = 1,
        Supporter = 2,
        Sponsor = 3,
        Sandbox = 4,
        Moderator = 5, // this is only used for command system
        Admin = 6,
    }
}
